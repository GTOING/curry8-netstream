# PR2 Rally 合同修复事实报告

日期：2026-09-21

## 执行范围

本次只修复 PR #2 快照中的合同措辞与对应验证要求，没有实现 Rally 控制、修改运行时代码或执行测试。工作副本为独立 clone：`/Users/xuqinghe/sleep/pr2-rally-contract-repair-20260921`；分支 `codex/rally-api-task-queue`；执行基线及核对到的 PR/远端分支 head 均为 `2b6ba29a740d80c29c11e0ab62712048e460bd79`。本地共享主 checkout 未改动。

## 修复内容

- `decision_records/ACTIVE_QUEUE.md`：区分正常分期决策资格与当前实时启用会话的故障/关闭保护性停止；明确默认未启用真实控制时的零请求范围，并把基线停止、关闭/退出停止和启动结果不确定时的补偿停止列为独立控制动作；补足单测及集成测试断言。
- `reference/CURRENT_TASK_QUEUE.md`：同步同一合同边界和测试矩阵，保持权威队列与执行快照一致。

现在应分别验证以下情形：

1. 默认未启用真实控制（包括默认 `NoModel` 与回放）时，无论分期结果如何均不得产生真实请求。
2. 不合格结果不能成为新的正常目标期/非目标期依据，也不能触发 `Start Stim`。当前实时、真实控制已启用的会话发生合同既定故障且设备未确认停止时，执行恰好一次有界保护性 `Stop Stim`，不发送 `Start Stim`；这是故障收尾，不是由该分期结果推导出的决策。
3. 旧 session/generation 的迟到失败、取消或过期结果，以及回放、离线和历史结果，不得改变当前会话状态或触发控制命令。
4. 显式启用时的基线停止、用户关闭/断开/退出停止、启动不确定后的补偿停止，各按其独立场景验证，不纳入“不合格分期结果零请求”断言。

## 历史证据处理

未改写 P3 合同及 P3、PR1 历史报告。它们记载的是既有模拟/默认场景的事实；其中 P3 报告还记录了当时 `NoModel` 条件下即使显式开启自动决策也没有 UDP 请求。这些记录保留为历史证据，不作为 PR #2 真实 Rally 故障收尾语义或其实现/验证结果的证明。

## 验证与未执行项

- 已执行 `git diff --check`；并对本报告及两份队列文档中的相对 Markdown 链接做存在性检查。
- 未运行 pytest、其他代码测试或构建；未启动应用、连接 Curry/Rally/硬件或进行真实刺激验收。
- 未提交、推送、合并或发布 PR 评论；未修改共享主 checkout。

本报告记录的是合同文档修复及静态文档检查，不代表 Rally 回复确认、物理输出确认或运行时行为已实现。
