你是现代化控制台 UI 收口执行线程。

在 `/Users/xuqinghe/sleep/main-development` 的现有 `feature/modern-control-ui` 分支继续完成[主线程首次复核](../../reports/P_GUI_MODERNIZATION_ACCEPTANCE.md)中的三项局部问题。开始核对 HEAD、dirty 和空 Git index；保留主线程原有设计／队列文档与本轮未提交实现，不切回 main、不重置、不清理。以[设计基线](../P_GUI_MODERNIZATION_DESIGN.md)为目标；不要改 Curry、ONNX 推理、Rally 状态机、记录格式或实验参数。

1. 修正固定栏的记录／CSV 简报：**已结束的会话不能显示“待连接”或“运行中”**。至少区分记录和 CSV 的未启用／已选择待连接／正在写入／已关闭或已写入完成／失败与不完整；如果只收到文本状态，使用已有 `_session_finished`、当前会话和控制状态判断，避免靠“写入”一个字词把“已写入 N 行”误判为仍在运行。详细原始状态仍保留在记录页与可访问提示。测试覆盖启用前、采集中、正常关闭（含零块／零行）、失败与重新连接新会话，以及回放完整／不完整。
2. 修正连接前模型简报：勾选 ONNX 但未连接时，显示“已选择、待连接校验／加载”，不说 NoModel 或已加载成功；取消勾选恢复 NoModel。连接后的成功／失败／NoModel、回放历史结果继续依据实际事件，不被配置变动冒充当前分期。增加相应 UI 测试。
3. 完成运行总览的视觉层级：在默认无会话状态提供简洁任务导向的空状态及**只切到**“连接／模型／诊断”页的入口；实时阶段把真实 30 秒累积进度和期别／无结果原因做成主信息，同时保留数值文字。整理重复文案、控件间距与固定栏狭窄换行；在 800×600、默认尺寸、长错误与 `FAULT/UNKNOWN` 下核对连接状态、API 最近确认、独立停止提示及关闭自动控制入口始终清楚。保持克制的 Windows 控制台风格，保留焦点、键盘和 Issue 8 滚轮行为。页面导航或刷新外观不得触发业务信号或任何 Rally 请求。

更新实际 UI／合成集成测试，覆盖上述两项状态复现、导航零请求与原有停用路径；最终运行 `uv sync --locked`、相关测试、完整回归、编译及 `git diff --check`。更新默认、800×600、真实模式故障、回放和长文本截图；截图生成仍只用显式合成状态。将结果**追加**到 `reports/P_GUI_MODERNIZATION_REPORT.md`，保留首次 251 passed 证据和未验证限制，并列出收口后的测试数与视觉变化。Windows 原生鼠标、DPI／高对比度如不可用仍标明未验证；不用真实 Curry、Rally/8801、COM、设备或受试者。

不要修改 `decision_records/ACTIVE_QUEUE.md`、主线程验收文档或旧报告；不要暂存、提交、推送、建 PR、合并或另发任务。完成后回报分支、HEAD、dirty、截图与事实证据供主线程复核。
