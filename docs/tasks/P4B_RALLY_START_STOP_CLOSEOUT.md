你是 P4-B 收口执行线程。在 `/Users/xuqinghe/sleep/main-development` 现有未提交实现上继续修复，不重置已有内容。

依据：docs/tasks/P4B_RALLY_START_STOP_EXECUTION.md 与 reports/P4B_RALLY_START_STOP_ACCEPTANCE.md。主线程复核尚未通过，报告中的三项 P1 均为本轮必须修复项。

1. 实现无新结果时的周期年龄保护及真正发送 Start 前的资格复核；缓存不能在等待 Stop 后无检查地再次启动。通过可注入时钟和显式 tick/调度器验证静默流过期，不设置新的研究阈值。
2. 重构必要停止与记录生命周期的交接：网络 EOF/断开/记录错误不能因 acquire_external_work 被拒而阻止 Stop；控制租约需覆盖最后 outcome 入队与后续补偿停止，不能先 release 再申请/写事件。保存仍可写时 session_finished 必须在最终控制事件之后；磁盘不可写时尽力停止并报告缺失证据，不能虚报记录成功。明确未开始网络、已开始网络、处理失败、用户退出所有路径，不锁内 join，不引入死锁。不要全局放开 pipeline 已关闭后的普通工作。
3. 实现停止资源预留，有限隔离 socket 容量不足时拒绝新的 Start 并解除控制，保持一次必要 Stop 可发送；覆盖启动超时补偿资源。保留迟到回复端口隔离，不用释放旧 socket 立即重用端口掩盖容量问题。
4. 新增原任务要求的合成 Curry TCP→受控分期→假 Rally→GUI/记录集成，以及不再来数据的过期、EOF/关闭、记录写入失败、在途 Start 停止优先、旧成功回复不确认 Stop、小容量耗尽测试。使用事件屏障/可控时钟，禁止连接生产8801或真实设备。

更新实际合同和运行说明中受修复影响的细节，追加执行报告保留193 passed历史；完成最终全量回归与 diff --check，回报修复证据及剩余限制。原“不提交、不推送、不修改共享队列、不真机”边界不变。不自行再分包。
