你是睡眠分期电刺激控制器项目的 P1 桌面与 Curry 脑电接入执行线程。

工作目录：`/Users/xuqinghe/sleep`。

## 目标

完成可启动的桌面程序，复用 Curry 客户端在后台握手、接收并显示最新完整 30 秒 EEG 数据块，提供真实的连接、错误、断开和退出状态。完成本任务内必要的局部修复、验证、文档与事实报告。

## 必读依据

1. 根目录 `AGENTS.md`：按执行线程角色工作。
2. `docs/P1_DESKTOP_EEG_CONTRACT.md`：本阶段唯一控制性接口、UI、数据和验收约定，必须完整读取。
3. `docs/SLEEP_STIM_REQUIREMENTS.md`：整体需求边界。
4. 根 `README.md`、`pyproject.toml`：目录和 uv 环境。
5. 按需阅读 `reference/desktop-ui-template/`、`curry8-netstream/` 实际源码，以及 `reports/2026-09-15_reference_review.md` 中 UI/Curry 部分。

计划位于 `decision_records/plans/SLEEP_STIM_DEVELOPMENT_PLAN.md`，本次只实施 P1。P0 已完成 P1 所需最小合同，后续模型、刺激与同步接口仍未冻结，不扩展实施。

## 实施范围与路径

- `src/sleep_stim_controller/`：正式 GUI、应用控制器、Curry 后台适配及启动入口。文件拆分和线程实现由你选择，保持简单。
- 根 `pyproject.toml`：注册 `sleep-stim-controller` 启动命令，将根 tests 加入测试发现；确有必要时调整依赖并同步 `uv.lock`。
- 根 `tests/`：新控制器和 GUI/合成集成测试，共用合成服务器辅助，不收集参考目录的脚本。
- `curry8-netstream/src/`、其 `tests/` 及必要说明：只允许为合同要求的取消、退出、会话通知和错误可观察性做最小兼容改动，保留既有正常 CLI 用法。此目录是独立 Git 仓库，不初始化主仓库或改变 Git index。
- 根 `README.md`：更新实际启动、验证、配置与当前限制。
- `reports/P1_DESKTOP_EEG_REPORT.md` 和必要的 P1 截图/日志：记录交付与证据。

保留模板、电刺激和设备说明原件，不修改 `reference/`。共享队列、决策文件、需求与控制性合同由主线程维护，本任务不修改。

## 具体工作

1. 核对现有工作区，保留其他人的改动。复用现有 uv `.venv`，读取合同后直接推进实现，不等待 Rally 或模型。
2. 按合同将 UI 模板转为正式桌面界面，提供可启动命令。删除模板中的无效占位操作，界面业务文案使用中文。
3. 接入现有 Curry TCP/协议能力，实现后台连接与接收、严格块校验、最新块绘图和有界显示交接。
4. 完成取消、异常、重连和关闭生命周期；保证空闲与半包可取消，错误可见且不与正常断开混淆。
5. 执行相称测试和窗口验证，失败在本任务范围内修复，最终运行包含既有 Curry 与新增测试的相关完整集合。
6. 更新使用说明和事实报告，交付后停止，不自动进入 P2 或分包。

## 不变项

模型不在本任务中；缺模型显示未接入，不生成分期。电刺激、目标睡眠期策略、真实设备通信、正式实验、时钟同步、记录/回放均不在本任务中。

不固定 EEG 通道数、采样率或微伏单位，不采用电刺激 Demo 的数值。30 秒数据校验、错误语义、UI 状态和测试边界严格以 P1 合同为准。实现细节可自主决定；改变这些语义时报告具体冲突和最小待决项，继续完成独立部分。

用户授权本任务的代码、局部修复、环境同步和本地合成验证；不包含 Git 提交/推送、建分支/工作树、启动正式采集或对外发送消息。

## 验证要求

在根目录使用 uv 管理环境。至少实际执行：

```sh
uv sync --locked
uv run --locked pytest
```

如果修改依赖，先正常更新锁文件，再运行上述命令。测试组合需满足合同第 6 节。纯离屏验证可设置 `QT_QPA_PLATFORM=offscreen`；不要将该设置强制写入正式 GUI 入口。

验证 `uv run --locked sleep-stim-controller` 能启动并正常关闭。GUI 工具不可用时记录实际采用的替代验证及未完成的原生观察，不谎称人工/真机检查。只运行本地合成服务，完成后清理本任务创建的进程。

## 回报

在 `reports/P1_DESKTOP_EEG_REPORT.md` 写明：

- 实现范围、文件清单和实际启动方式。
- 合同各主要验收项对应的代码/测试/截图证据。
- 实际运行命令、通过/失败数量、修复结果；未执行项目必须明确。
- Curry 上游库具体改动及兼容性影响。
- 静默/半包取消、错误传播、重连和关闭的实测结果，后台资源是否退出。
- 剩余限制和需要主线程判断的最小问题。

最终回复提供报告链接、启动命令、验证摘要及任何真实阻塞。不得把合成数据接收通过描述为 Curry 真机已验证，也不得更新共享队列声称已被主线程验收。

## 主线程验收补充（2026-09-15，原 P1 内继续）

P1 主要实现已收到。请继续阅读 `reports/P1_DESKTOP_EEG_ACCEPTANCE.md`，按其中证据完成以下收口：

1. 修复会话结束后 pending 数据经 poll_latest/show_block 覆盖“历史数据”标注的问题，覆盖 EOF、主动断开和重连时序。
2. 补齐等待握手、帧头/载荷半包取消、活跃连接窗口关闭的测试，并实际确认后台线程和连接已释放。
3. 补齐采样不连续及非有限数据通过真实合成 TCP 到可见 error 的证据，检查最小窗口的真实布局。
4. 完成局部修复后运行相关完整回归，向原执行报告追加本次结果，保留此前 24 passed 记录。

这属于原合同内局部修复和必要验证，不需重启 P1 或进入 P2。原生 GUI 被工具策略限制时继续如实记录，不绕过限制。完成后交主线程复核，执行线程不修改共享队列或验收记录。
