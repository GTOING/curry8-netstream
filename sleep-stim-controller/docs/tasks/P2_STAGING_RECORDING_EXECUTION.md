你是睡眠分期电刺激控制器项目的 P2 分期接口、会话记录与离线回放执行线程。

工作目录：`/Users/xuqinghe/sleep`。

## 目标与依据

在已验收的 P1 上完成可插入的分期接口、默认无模型状态、用户主动启用的会话记录，以及通过正式 UI 查看已保存数据和结果的只读回放。

先阅读根 `AGENTS.md`，并完整读取 `docs/P2_STAGING_RECORDING_CONTRACT.md`。这是本任务控制性合同。恢复 P1 必要上下文时阅读 `reports/P1_DESKTOP_EEG_ACCEPTANCE.md` 最后的收口结论、现有 `src/sleep_stim_controller/` 和相关测试。需求和总计划分别位于 `docs/SLEEP_STIM_REQUIREMENTS.md`、`decision_records/plans/SLEEP_STIM_DEVELOPMENT_PLAN.md`；本次只实施 P2。

## 范围与路径

- `src/sleep_stim_controller/`：类型/分期协议、后台处理、记录读写、回放、GUI 和应用接线。保留 P1 网络与显示能力，不复制协议解析器或新建第二个完整应用。
- `tests/`：扩展既有合成服务和测试辅助，加入本阶段行为验证。测试替身和合成数据只能用于测试。
- 根 `README.md`：实际保存、回放和模型未接入说明，更新目录与使用命令。
- 根 `pyproject.toml`、`uv.lock`：仅在实现确实需要时修改。优先现有 NumPy/PySide6/标准库，无需安装模型框架、数据库或 Rally。
- `reports/P2_STAGING_RECORDING_REPORT.md`、必要 P2 截图及小型证据文件：实际实现、测试和限制。
- `curry8-netstream/` 原则上复用；确有 P2 接收交付或取消必要的局部扩展时允许修改源码与相关测试，必须记录兼容性影响，不能改变线协议和单位。

不得修改 reference 原件、P1 历史报告、控制性合同、需求、决策和共享队列。根目录与 Curry 的 Git 边界保持现状，不提交/推送、建分支/工作树或初始化仓库。

## 工作步骤

1. 核对工作区并保留已有改动；读取合同后选择简单模块划分和线程所有权。
2. 在 P1 校验后、最新块替换前接入顺序后台处理；先实现块关联、默认无模型和可注入测试适配器，保证 P1 显示丢帧不影响处理与保存。
3. 实现 v1 会话读写与失败/中断前缀读取，再接入手动保存和逐块只读回放。生产默认保存关闭，不能自动记录用户目录。
4. 接入分期与保存状态 UI，保留 P1 操作可见性、终态历史语义与退出清理。
5. 按合同第 7 节执行有效测试组合，局部失败在任务内修复；最终运行包含 P1/Curry 的相关完整回归，更新说明并完成报告。

实现过程中可自主选择文件拆分、队列容量和局部方法；在报告中解释处理上限与退出策略。不要将测试 fixture 参数变成正式实验参数。改变数据/模型/记录合同需报告最小冲突，先完成独立部分。

## 不变项

无真实模型时必须显示未接入，分期为 unavailable，不默认 W。不制作生产环境假模型、模型文件加载器或通用插件平台。真实模型预处理要求留待后续接入。

本任务不实现目标睡眠期触发策略、Rally 调参、实际启停或时钟同步，不运行真实 Curry/Rally、归档刺激脚本、训练或受试者实验。回放仅展示已记录内容，禁止网络控制和重新推理。

P2 校验后的数据文件是解码后的原值，不是线协议抓包；单位仍未知。记录默认关闭，不将 UI 最新块缓冲当作保存/推理队列。存储失败和积压不可以静默降级为“记录成功”。

## 实际验证与收尾

使用现有 uv 环境。必要时更新锁文件，随后实际执行：

```sh
uv sync --locked
QT_QPA_PLATFORM=offscreen uv run --locked pytest
```

uv 缓存权限按当前工具能力正常处理，不改变用户系统配置。GUI 正式入口不强制 offscreen。验证 `uv run --locked sleep-stim-controller` 可启动，并通过真实窗口接线验证记录/打开回放/退出与后台资源回收。

合成测试只绑定本机回环；文件输出使用测试临时目录，正式截图和报告在 reports。进程和写者在测试完成后退出；部分输出和失败证据依合同保留。没有真实设备或原生 GUI 访问能力时报告限制，不将模拟结果称为真机验收。

在 `reports/P2_STAGING_RECORDING_REPORT.md` 中记录：

- 实际实现文件、公开模型接口与会话格式、UI 使用路径。
- 每项主要验收标准的源码/测试/截图依据。
- 队列/内存边界、积压、取消和写入失败的行为与实测结果。
- 实际命令与测试数量，失败原因、修复和最终回归结果；未执行项单列。
- 未接入模型、未真机、无同步和刺激的限制；任何必要后续决策。

最终回复给报告和截图链接、启动方式、验证摘要及真实阻塞。你不维护共享队列，不自行宣告主线程验收或进入 P3，也不再分包。

## 主线程验收补充（2026-09-16，原 P2 内继续）

已收到主要实现与 49 passed 回报。先阅读 `reports/P2_STAGING_RECORDING_ACCEPTANCE.md`，修复实时 pending 数据在回放模式被标成离线回放、覆盖所选会话曲线的问题。

在实际应用模式切换边界隔离实时绘图缓冲与回放，保留 P1 断开后最后一块的正常历史查看。补充确定性接线测试：停止绘图轮询保留实时 pending → 结束实时连接 → 打开不同数据的回放 → 再触发绘图泵，验证显示与结果来源一致；空/失败回放及退出回放也不得出现旧实时数据。

完成后在最终代码上运行相关完整回归，向原执行报告追加实际结果。保留原 49 passed 和原生 macOS 启动 139 的失败事实；没有新证据时不推断其根因或声称原生已通过。无需新建阶段任务，不进入 P3，不修改共享队列与验收记录。
