你是睡眠分期电刺激控制器项目的 P-GUI 界面整理与优化执行线程。

工作目录：`/Users/xuqinghe/sleep`。

## 目标与依据

执行界面改造：用户在 Curry 8 查看 EEG，本应用移除右侧及回放脑电图，整理为围绕分期与刺激控制的紧凑桌面控制台。

先读 AGENTS.md 和 `docs/P_GUI_CONTRACT.md`；以该合同替代旧 P1/P2 中的波形展示要求。按需读 P3 合同及 P4-A 验收，核对当前 ui.py、app.py、controller.py、replay.py 和相应测试。不要重新执行历史任务。

## 修改范围

允许修改 `src/sleep_stim_controller/` 内必要 UI、摘要交接、回放接线和诊断文案，`tests/` 的相关断言，`README.md`，必要的 `pyproject.toml`/`uv.lock`，以及 `reports/P_GUI_REPORT.md` 和 `reports/P_GUI_*` 截图/证据。

不修改合同、决策、共享队列、历史报告、reference 或独立 Curry 仓库；不初始化 Git、提交/推送、创建分支/工作树，不再分包。

## 实施要求

1. 移除 PlotPanel/绘图画布及绘图专用代码，正式入口不再构建 PyQtGraph/QGraphicsView；不要仅 hide 或留空占位。审查其他引用后按需移除根 PyQtGraph 依赖。
2. 按合同重排全窗口信息层级：固定连接/模式/错误区，清晰的数据分期摘要、刺激配置、会话记录/回放区；采用一致字体间距和对齐，确保最小窗口、长文本与滚动操作可用。
3. 复用现有有界摘要交接与后台数据链路，保留实时/历史/回放标记、session/block 关联与所有模式隔离。消除绘图降采样/数组工作和过时提示，不改变处理/记录/刺激语义。
4. 回放保留逐块加载和导航，以样本范围、分期和历史刺激事件展示；数据验证不因不绘图被跳过。空/损坏会话不能残留旧会话摘要。
5. 更新已失效的曲线测试为摘要关联测试，保留测试原本覆盖的状态、异步覆盖和资源清理风险。完成必要 UI/集成验证、最终完整回归和截图。

普通布局细节自行选择，无需逐项确认；不扩展为新业务功能或真机控制。真实 Curry/Rally、模型、设备刺激、同步、受试者实验均不在范围，网络测试仅本机合成 TCP 与应用拥有的随机端口 UDP。

## 验证与回报

实际执行：

```sh
uv sync --locked
QT_QPA_PLATFORM=offscreen uv run --locked pytest
```

有必要的依赖变化先更新锁文件。若失败，在范围内修复后完成最终完整回归，保留实际失败和修复记录。不要以旧 88 passed 代替本轮结果，也不以减少测试数本身判定失败；说明哪些断言因取消绘图被替换及保留的覆盖。

报告 `reports/P_GUI_REPORT.md`：实际改动、布局说明、后台不变证据、去图/依赖结果、测试与命令、默认和 800×600 截图、回放与长文本验证、原生尝试结果或未执行原因。已有 P4-A 零屏幕/139 历史保持不变；本轮去图不能直接宣称原生已修复。运行标准入口仍为 `uv run --locked sleep-stim-controller`。

最终回复报告链接、截图、实际验证结果和未解决项。不要修改队列或宣称主线程已验收，不自动进入 P4 真机或 P5。
