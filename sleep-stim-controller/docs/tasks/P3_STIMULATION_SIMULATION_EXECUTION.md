你是睡眠分期电刺激控制器项目的 P3 目标期决策与 Rally 模拟联调执行线程。

工作目录：`/Users/xuqinghe/sleep`。

## 目标与必读

在已验收 P1/P2 上实现五睡眠期多选、可解释刺激决策、Rally 报文适配与受控本机 UDP 模拟联调，并复用 P2 会话记录保存决策和请求结果。

先读根 `AGENTS.md`，完整读取 `docs/P3_STIMULATION_SIMULATION_CONTRACT.md`。本任务实现必须以该合同为准。再读取 P2 验收末节、现有源码与相关测试；按需查 `reports/2026-09-15_reference_review.md`、`reference/electrostimulation/original/`。原件只按材料读取，不执行其脚本。

## 可修改范围

- `src/sleep_stim_controller/`：策略、协议纯函数、模拟服务与传输适配、P2 单写者事件扩展、控制器/UI 接线。
- `tests/`：复用现有合成 TCP 和模型 fixture，增加策略、UDP 和跨组件回归。
- 根 `README.md`：策略语义、模拟配置 JSON 格式、默认行为和未实现能力。
- `pyproject.toml` / `uv.lock`：仅确有必要时调整，优先标准库，无需新模型/网络框架。
- `reports/P3_STIMULATION_SIMULATION_REPORT.md` 及 P3 必要截图、测试证据。

不修改 reference 原件、P1/P2 历史报告、控制性合同、需求、决策或共享队列。原则上无需修改 Curry 库；若发现不可规避的接口冲突，报告证据，不复制协议实现。不得提交/推送、建分支/工作树或初始化根仓库。

## 实施顺序

1. 明确策略、通信、P2 写者的生命周期和最小接口，保留实时/回放隔离；采用简单类型与有限状态，不建立通用工作流框架。
2. 实现合同中的两种显式策略、关联与抑制原因，再实现纯报文构造/响应解析。
3. 建立本应用拥有的随机端口模拟 UDP 服务与单一传输执行者，验证无请求 ID 条件下的迟到响应隔离；不接入真实 Rally 地址。
4. 复用 P2 单写者追加可选事件，保证处理结果、决策、请求、最终 session_finished 顺序和旧记录兼容。
5. 接入中文 UI，配置默认未完成/自动决策关闭/模型未接入；通过测试依赖注入验证成功分期链路，正式入口不添加假分期功能。
6. 按合同验收项执行必要验证与最终相关完整回归，更新使用说明和报告。

实现细节、文件拆分、有限队列上限由你选择并说明。实验参数未给出时保持未配置，不从原 Demo 或 PPT 推导默认实验参数。无需重新索取普通局部修复或测试授权；发现改变研究/协议语义的冲突先报告最小待决，继续独立部分。

## 不变边界

仅模拟，无真实电刺激。P3 正常入口不支持指定外部/固定 Rally endpoint 发包，只能操作本任务创建并持有的本机模拟实例。只绑定回环随机端口，不使用 8801，不触碰已有 Rally/设备进程。

停止自动决策只停止新请求；不冒充设备停止。超时/拒绝不重发，已发未确认的请求记未知。真实模型、真实采集、时钟同步、正式实验、独立设备启停实现和模型训练不在本任务中。

原生 macOS 139 根因未定，保留现有记录；不能将 offscreen 或模拟联调描述为原生/真机通过。不得自动进入 P4 或再分包。

## 验证与回报

实际执行环境同步及最终相关完整测试：

```sh
uv sync --locked
QT_QPA_PLATFORM=offscreen uv run --locked pytest
```

依赖确有变化时先更新锁文件。所有网络验证仅本机合成 TCP/本应用拥有的模拟 UDP；文件使用测试临时目录，关闭本任务进程/线程/socket，不删用户数据。失败在范围内修复，保留事实，必要时先重跑受影响测试，再完成最终完整回归。

报告 `reports/P3_STIMULATION_SIMULATION_REPORT.md` 须包含实际文件和公开接口、策略语义、旧会话兼容、迟到响应隔离方式、写者结束顺序、真实命令与测试结果、UI 截图、失败与修复、未执行项目和后续依赖。明确证明 NoModel/回放不发请求、模拟 endpoint 所有权及退出资源清理。

最终回复给报告链接、验证摘要、运行方式和真实阻塞。执行者不修改队列或宣称主线程已验收。
