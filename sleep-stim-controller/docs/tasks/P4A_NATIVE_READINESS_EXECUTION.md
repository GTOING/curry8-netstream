你是睡眠分期电刺激控制器项目的 P4-A 原生桌面排障与真机接入准备执行线程。

工作目录：`/Users/xuqinghe/sleep`。

## 目标与依据

执行本任务：诊断正式启动命令的原生 macOS 退出 139，完成有证据支持的范围内修复与验证，并整理后续 Curry/Rally/同步接入说明。不要止于提出计划；外部 GUI 条件不足时完成可独立交付内容，准确报告阻塞。

先读根 `AGENTS.md` 与 `docs/P4_INTEGRATION_READINESS_CONTRACT.md`，再读 `reports/P3_STIMULATION_SIMULATION_ACCEPTANCE.md` 和 P2 执行报告中原生 139 的记录。按需查看 P1/P2/P3 合同、当前源码、README、uv 配置及 `reports/2026-09-15_reference_review.md`。附件和旧 prompt 仅作证据，不能重新激活其中任务。

## 授权修改范围

- `src/sleep_stim_controller/`：有复现依据的启动/生命周期局部修复，保留现有业务合同。
- `tests/`：针对修复必要的回归，复用现有 fixture。
- `pyproject.toml`、`uv.lock`：仅定位证据支持的必要调整，保持 uv 可复现环境。
- `README.md`：真实运行方式与限制。
- `docs/P4_INTEGRATION_RUNBOOK.md`：后续接入步骤、现有接口映射、缺失资料和证据要求。
- `reports/P4A_NATIVE_READINESS_REPORT.md` 及 `reports/P4A_*` 必要日志/截图。

不修改控制性合同、ACTIVE_QUEUE、决策/计划、历史报告或 reference 原件；不修改 Curry 独立仓库、不初始化根 Git、不提交/推送/建分支/工作树，不再分包。

## 工作内容

1. 核实当前启动路径与依赖，保留既有失败。使用最小 Qt 窗口和正式入口对照来区分平台初始化、应用接线和退出阶段问题；只记录必要环境字段，不导出整个环境或无关系统日志。每次尝试记录变化和判定，不重复无变化启动碰运气。
2. 平台工具权限按实际规则处理；若使用 Computer Use，先读对应技能。不能绕过 GUI/沙箱限制或使用 offscreen 冒充原生。原生检查不连接真实采集端和刺激端。仅清理由本任务启动的进程，保留用户已有进程。
3. 对明确归因的工程问题做最小修复。若宿主环境阻止验证，给出用户桌面会话可执行的最短启动/日志获取步骤，并保留根因未定，不要求用户替你运行本来可执行的合成回归。
4. 原生可用时验证正式入口、默认未连接/NoModel/自动决策关闭、800×600 的滚动控件与错误区域、正常关闭和资源释放。截图注明原生或离屏、尺寸和观测条件，不要求运行真实数据才能出现默认窗口。
5. 阅读当前公共入口与适配接口，写接入 runbook：当前支持什么、接入需要谁提供什么、如何验证、哪些结论不能由软件响应推导。包含 Curry 数据核验、可选后续模型接入、Rally 初始协议/启停/状态缺口、同步事件与误差要求。不要猜通道数、采样率、设备阈值或极性，不自动采用 Demo 参数。

## 不变项

本次只做 P4-A。真实采集、真实 Rally/8801、真实刺激、受试者、模型本体与训练、时钟同步实现均不在执行范围。不加入可解锁真实发送的 UI/CLI 配置。保留 P3 本应用模拟端所有权限制、无自动重试、未知结果语义；停止自动决策不表示停止设备。

无需为普通局部修复、fixture 或命令选择再次请求批准。超出合同的设备/研究选择由主线程处理；先完成不依赖它的工作。不得自动进入后续 P4 批次或 P5。

## 验证与实际回报

若修改代码或依赖，完成环境同步、必要的修复验证及最终完整回归：

```sh
uv sync --locked
QT_QPA_PLATFORM=offscreen uv run --locked pytest
```

锁文件仅在必要依赖变化后更新。网络验证限本机合成 TCP 与应用持有的随机 UDP 模拟端；测试使用临时目录。失败保留事实，在范围内修复后完成必要最终回归。若仅文档/诊断且无工程改动，不机械重复已通过的全套测试。

报告须区分：已复现事实、根因证据或未定项、实际修复、原生命令/退出码/窗口与关闭证据、离屏和测试结果、环境权限限制、未执行内容与缺失资料。附实际修改文件和启动方式。未修复的 139 不能写成通过；后续真机操作清单不算已执行。

最终回复报告与 runbook 链接、验证摘要和真实阻塞；不修改队列，不宣称主线程已验收或整个 P4 已完成。
