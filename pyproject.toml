[project]
name = "curry-netstream"
version = "0.1.0"
description = "Curry 8 NetStreaming 调试客户端骨架（Python）"
requires-python = ">=3.11"
dependencies = ["numpy"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-v"
